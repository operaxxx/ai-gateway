# Agent / LLM Gateway：系统化分析框架

> 本文是对「生产级 LLM 接入」的结构化思考。目标不是罗列功能，而是回答三个问题：
> 1. Gateway 为什么存在（根本矛盾）？
> 2. 它在架构上由哪些部分组成，彼此什么逻辑关系？
> 3. 每一项治理策略解决什么矛盾、如何实施、代价是什么？

---

## 0. 定位：Gateway 为什么存在

Gateway 是 **Agent 应用（下游）与 LLM 供应商（上游）之间的中间层**。
它存在的理由来自两个事实：

1. **上游是异构且不可控的第三方服务**：多供应商、多协议、多版本，SLA、限流、定价都不受你控制；
2. **LLM 调用不是普通 RPC**：非确定性、流式输出、长耗时、按 token 计费、携带上下文状态、有内容风险。

由此推出 Gateway 的全部职责，可以用「五对矛盾」作为贯穿全文的主线：

| 矛盾 | 下游想要 | 对应的 Gateway 职责 |
|---|---|---|
| 上游**异构** | 稳定统一的契约 | 协议适配、语义归一 |
| 上游**不可靠**（慢/挂/限流/涨价） | 稳定可用 | 超时、重试、幂等、熔断、降级、故障转移 |
| 调用**昂贵且按量计费** | 成本可控 | 缓存、配额、多 key、模型路由、token 核算 |
| 内容**黑盒、数据外泄风险** | 安全合规 | 密钥收敛、脱敏、审核、审计 |
| 行为**非确定、链路长、难调试** | 可解释、可优化 | LLM 专属可观测性 + Prompt 资产管理 |

普通微服务网关要做的事（统一接入、鉴权、限流、观测）它都要做；**协议翻译、token 经济、流式治理、内容治理是 LLM Gateway 的增量命题**。

---

## 1. 全景：分层架构

```
                        ┌─────────────────────────────────────────────┐
   下游 Agent / 前端 ──▶ │  接入层 Northbound                            │
                        │  统一 API(OpenAI 兼容形态) · 鉴权 · 租户 · 校验 │
                        └──────────────────────┬──────────────────────┘
                                               │
                        ┌──────────────────────▼──────────────────────┐
                        │  资产与路由层                                 │
                        │  Prompt 模板/版本 · 模型路由表 · 策略配置       │
                        └──────────────────────┬──────────────────────┘
                                               │  统一领域模型（内核）
                        ┌──────────────────────▼──────────────────────┐
                        │  ChatRequest / Message / ChatResponse /       │
                        │  StreamEvent / Usage        （协议无关）       │
                        └──────────────────────┬──────────────────────┘
                                               │
              横切治理链 ──▶ ┌─────────────────▼──────────────────────┐
   缓存 · 配额限流 · 幂等     │  适配器层 Southbound（入向/出向双向翻译）   │
   重试 · 熔断 · 降级 · 记账  │  OpenAI ChatCompletions / Responses ·     │
                            │  Anthropic Messages                       │
                            └──────────────────────┬────────────────────┘
                                                   │
                                            各家 LLM 上游

       可观测性（指标 / 日志 / Trace）贯穿所有层，在每一跳打点
```

分层的核心原则：

- **内核协议无关，适配器协议私有**。统一模型是全系统唯一的「通用语言」，新增供应商 = 新增一个适配器，内核与上层零改动。
- **横切治理与业务翻译正交**。缓存、限流、熔断等是包裹在调用链路上的「中间件链」，适配器本身保持薄。
- **数据面与控制面分离**。请求流转是数据面；Prompt、路由表、限流/降级开关是控制面，应可配置、可热更新。

---

## 2. 请求生命周期：各层如何串联

一次非流式 `/v1/chat` 的完整路径：

```
认证鉴权 → 请求校验 → Prompt 引用解析与服务端渲染 → 路由(model→adapter)
  → 入向协议翻译 → 缓存判断 → 配额/限流检查 → 熔断状态检查
  → 发起上游调用(超时/重试) → 出向翻译(错误归一、stop_reason 归一)
  → 结构化输出校验 → 记账(tokens/成本)与指标打点 → JSON 交付
```

### 流式生命周期的四个特殊点（与非流式的本质差异）

1. **错误通道改变**：HTTP 200 和响应头一旦发出，就不能再用状态码报错。
   流中途的错误必须**事件化**（统一 `StreamEvent(type="error")`），因此错误处理天然是「双轨制」。
2. **中继（relay）vs 缓冲（buffer）**：边收边转延迟最低，但无法对完整结果做缓存/Schema 校验
   （本项目因此明确禁止 `stream=true` 与 `response_format` 组合）。缓冲则相反。这是流式最根本的权衡。
3. **计时口径必须在网关层统一打点**：TTFT/总时长不能采信厂商字段，否则口径随适配器漂移。
4. **连接的生命周期管理**：代理缓冲会破坏实时性（需 `Cache-Control: no-cache`、`X-Accel-Buffering: no`）；
   客户端主动断开必须**级联取消上游请求**，否则白烧 token；还要处理上游半连接、SSE 非法帧的容错。

---

## 3. 接入上游的五件事：逐项深入

### 3.1 统一接口转换（协议适配）

**异构是一个笛卡尔空间**，而不是单个维度：

```
2 套协议标准(OpenAI / Anthropic)
  × 2 种 API 形态(chat completion / responses)
  × 2 种响应模式(stream / non-stream)
```

适配器要覆盖这个矩阵，且翻译是**双向**的：入向（统一模型 → 厂商 payload）、出向（厂商响应/事件 → 统一模型）。

差异不止是字段名，更多是**语义结构鸿沟**：

| 语义点 | OpenAI Chat Completions | Anthropic Messages | OpenAI Responses |
|---|---|---|---|
| system 提示 | messages 数组中的一条 | 顶层独立 `system` 字段 | 顶层 `instructions` |
| token 上限 | `max_tokens`（可选） | `max_tokens`（**必填**） | `max_output_tokens` |
| 鉴权 | `Authorization: Bearer` | `x-api-key` + `anthropic-version` | Bearer |
| 用量字段 | prompt/completion_tokens | input/output_tokens | 用法各异 |
| 停止原因 | finish_reason: stop/length/… | stop_reason: end_turn/max_tokens/… | status + 各自枚举 |
| 流式外壳 | SSE（chunk 结构不同） | SSE（event 名不同） | SSE（事件语义不同） |

设计要点：

- **网络 IO 与纯翻译分离**：`complete()`/`stream()` 只负责通信，`_build_payload()`、`_parse_response()` 是纯函数，可脱离网络单测。
- **枚举归一策略 = 统一词表 + 未知值透传**：认识的值翻译成统一词表（stop / max_tokens / content_filter / error），
  不认识的原样透传，以向前兼容厂商新增枚举；不要把未知值硬塞成 `error`。
- **信息损失要可见**：归一化必然丢信息，保留 `ChatResponse.raw` 存原始响应供调试/对账。
- **中转站兼容**：base_url 与 path 可环境变量配置，并用 `rstrip("/")` 消除斜杠歧义。
- **SSE 外壳是公共能力**：SSE 帧解析与业务协议无关，应抽成公共模块（`sse.py`）被各适配器复用，差异只在事件语义映射。

### 3.2 统一异常处理

先按**错误来源分层**，再谈翻译：

| 来源 | 典型错误 | retryable? |
|---|---|---|
| 客户端 | 参数非法、模型不存在、上下文超长(400)、余额不足(402) | 否 |
| 上游 | 429 限流、529/503 过载、5xx、超时 | 部分是（429/5xx/超时） |
| 网关自身 | 配置缺失、模板渲染失败 | 否（fail fast） |
| 流式中途 | 上游断流、生成中报错 | 视情况 |
| 后处理 | 结构化输出不符 Schema（422） | 否（或触发重试生成） |

设计要点：

- **领域异常各层自定义，只在 HTTP 边界翻译成状态码**。内核不感知 HTTP，不允许 `httpx.HTTPStatusError` 直接泄漏给调用方。
- **上游错误归一为统一错误模型**，建议至少含：`type`（auth / rate_limit / quota / overloaded / timeout / context_length / content_filter / server）、`retryable`、`upstream_status`、`upstream_request_id`、`message`。这是重试、熔断、降级做决策的依据。
- **流式双轨**：建连前的错误走 HTTP 状态码；建连后的错误走 error 事件。两条路径要共用同一个归一化函数，避免错误口径分裂。
- **容错不等于吞错**：SSE 解析跳过坏帧是合理的容错，但必须计数上报，否则故障不可见。
- **fail fast 边界**：API key 缺失、Prompt 变量缺失、模板语法错误，一律在请求发往上游前拦截，不浪费一次昂贵调用。

### 3.3 幂等性处理

**LLM 调用天然不幂等**：生成有随机性，重试可能得到不同结果，还可能造成重复扣费。因此幂等要在网关显式构造。

机制：客户端传 `Idempotency-Key`，网关维护 `(key, 请求指纹) → 响应` 的映射（通常存 Redis，带 TTL）。

关键细节：

- **请求指纹**：model + messages + 采样参数（temperature / max_tokens / response_format 等）全部参与 hash；同 key 不同指纹应判冲突（409）。
- **in-flight 去重（singleflight）**：相同 key 的并发请求合并为一次上游调用，其余等待同一结果，防止超时瞬间的重试风暴击穿上游。
- **作用范围**：严格幂等只适用于非流式；流式可在网关缓冲完整流后按 key 重放（牺牲首字延迟换幂等），需权衡。
- **与重试的关系**：幂等键让「超时后不确定是否已扣费」的重试变得对上游安全，是重试机制的前置条件。
- 注意 `temperature=0` / `seed` 只提供**有限的可复现性**，不是真正的幂等，不能替代幂等键。

### 3.4 降级处理（fallback / degradation）

触发条件：上游过载、限流、持续超时、成本超预算、或质量不达标。
降级的**层次**（代价由小到大）：

1. **同语义故障转移（failover）**：换 key、换实例、换供应商跑同一个模型语义 —— 严格说这是 failover，不是降级；
2. **模型降级**：pro → flash（跨厂商时需评估语义/能力兼容性，尤其 tools/reasoning 行为差异）；
3. **能力降级**：关闭工具调用/推理通道、截断或摘要上下文、降低 max_tokens；
4. **质量降级**：接受缓存答案、缩短生成；
5. **静态兜底**：固定回复、排队提示、明确告知稍后重试。

设计要点：

- 策略**声明式配置**（什么模型在什么信号下降到哪一级），与代码分离；
- 对调用方**透明但可感知**：响应中带 `served_by` / `fallback` 标记，让 Agent 知道自己拿到的是降级结果；
- **Agent 场景慎用能力降级**：函数调用、结构化输出被降级可能直接打断 Agent 的执行流程，这类请求宁可失败也不要静默降级。

### 3.5 熔断处理（circuit breaker）

目的：上游持续故障时**快速失败**，避免请求堆积耗尽网关的连接池/线程，同时给上游恢复时间。

状态机：

```
 Closed（正常放行）
   │  错误率 / 慢调用率达到阈值（滑动窗口统计）
   ▼
 Open（直接快速失败，不打上游）── 冷却时间到 ──▶ Half-Open
 Open（直接快速失败，不打上游）◀── 探测失败 ── Half-Open（只放少量探测请求）
                                                │ 探测成功
                                                ▼
                                             Closed
```

LLM 场景的关键点：

- **熔断粒度**：按 `provider × model`，甚至细化到单个 API key —— key 被限流 ≠ 供应商整体故障；
- **统计口径要排除客户端错误**：context_length 超限（400）是请求本身的问题，计入熔断会造成误熔；
  重点信号是 429 / 529 / 5xx 占比和 TTFT 飙升；
- **与降级协同**：Open 期间不再打上游，请求直接进入降级链；Half-Open 只放行探测流量；
- **参数权衡**：阈值、窗口、冷却时间要和上游限流节奏匹配，避免自身雪崩或长期半开。

### 3.6 重试与超时（3.3–3.5 的共同底座）

- **超时要分层**：连接超时、首字节/读取超时、总超时；流式请求的「总超时」应按整个流生命周期计；
- **只重试可重试错误**（429 带 Retry-After、5xx、超时、连接错误），4xx 不重试；
- **指数退避 + 抖动（jitter）**，避免重试同步化形成风暴；
- 重试次数计入预算，配合幂等键；流式建连前可重试，建连后一般只降级不重放。

---

## 4. 可观测性：通用指标 + LLM 专属指标

通用服务指标回答「服务健不健康」：QPS、Latency（必须看 p50/p95/p99，均值会掩盖长尾）、
Error Rate、Throughput、Memory/连接池等资源占用。

LLM 专属指标回答「这门生意运转得好不好」：

| 指标 | 含义 | 为什么重要 |
|---|---|---|
| **Context** | 输入 token 数分布、上下文窗口占比、超长失败率 | 决定成本与能否命中窗口；超长是常见失败源 |
| **TTFT** | Time To First Token，发起请求 → 收到首个 token | 流式体验的第一指标，主要受排队/网络/前缀长度影响 |
| **TTLT** | Time To Last Token，发起请求 → 流结束（≈ 总耗时） | 反映总延迟；与输出长度强相关 |
| **ITL** | Inter-Token Latency，token 间间隔 | 衡量流是否「卡顿」，TTFT 正常但 ITL 抖大体验仍差 |
| **Token Usage** | input/output tokens，按 租户/模型/Prompt版本 分摊 | 计费、配额、成本归因的原始数据 |

衍生的**经济与质量指标**：每请求成本、输出吞吐（tokens/s）、缓存命中率、
结构化输出失败率、重试率、拒答/内容过滤率。

三条实施原则：

1. **打点在网关层统一完成**（本项目 TTFT/elapsed 即在 Gateway 层打点），口径不随厂商字段漂移；
2. **日志带 upstream request_id**，并对 input/output 内容留存做合规评估（可能含敏感数据）；
3. **Trace 透传**：下游 trace id → 网关 → 上游 request id 串联，才能跨边界定位一次慢请求。

---

## 5. 四个关键设计权衡（分析视角）

1. **透明透传 vs 强契约网关**
   透传（裸反代）最省事、永不落后于厂商迭代，但拿不到任何治理抓手（无法归一、无法记账、无法校验）；
   强契约（本项目路线：统一模型 + 适配器）治理深、下游稳定，代价是要持续追赶厂商新字段。
   折中：强契约为主，未知枚举透传 + raw 兜底，减少维护负担。

2. **中继 vs 缓冲**
   中继延迟最低、天然支持无限长流，但无法做整包校验/缓存/重试，错误只能事件化；
   缓冲能做完整治理，但牺牲 TTFT 且占内存。按接口类型选择，不要试图一套通吃。

3. **成本 – 延迟 – 质量三角**
   模型路由、缓存、降级、采样参数，本质上都是在这个三角上做动态选择。
   治理策略应显式表达「在什么信号下牺牲哪一角」，而不是埋在代码里。

4. **无状态网关 vs 有状态上游**
   Responses API、Agent 会话天然带状态（服务端存任务/上下文）。
   状态若沉淀在网关（会话、幂等表、缓存），会影响水平扩展；
   网关应尽量保持无状态，状态外置到 Redis/对象存储，会话状态尽量留在上游或下游。

---

## 6. 实施路线（对照本项目现状）

| 阶段 | 内容 | 现状 |
|---|---|---|
| **P0 统一契约** | 协议无关内核、三协议适配器、SSE 中继、stop_reason 归一、raw 兜底、计时打点、Prompt 版本化与服务端渲染、结构化输出校验、边界异常翻译 | ✅ 已实现 |
| **P1 可靠性** | 统一上游错误模型（捕获 httpx.HTTPStatusError 并归一）、分层超时、有限重试（退避+jitter）、按 provider×model 熔断 | ⬜ 部分缺口：非流式上游错误目前直接冒泡 |
| **P2 弹性与经济** | 多 key 池轮换、配额/限流（令牌桶 per 租户/模型）、声明式模型路由与降级链、幂等键 + singleflight、响应缓存 | ⬜ 未开始 |
| **P3 可观测** | Prometheus 指标（TTFT/TTLT histogram、tokens/errors counter）、结构化日志、trace 与 upstream request_id 串联 | ⬜ 仅有内存中的计时字段 |
| **P4 平台化** | 控制面热更新、多租户与鉴权、审计日志、PII 脱敏与内容审核、语义缓存 | ⬜ 未开始 |

演进原则：**先统一契约，再做可靠性，再谈经济与观测，最后平台化**。
顺序不能反——没有统一模型，治理逻辑就会在 N 套协议上各写一遍；没有可靠的错误归一，熔断和降级就没有决策信号。

---

## 7. 术语速查

- **Adapter（适配器）**：统一模型与某一厂商协议之间的双向翻译层。
- **Relay / Buffer**：流式的两种处理模式，边收边转 / 收齐再处理。
- **TTFT / TTLT / ITL**：首 token 延迟 / 末 token 总时长 / token 间间隔。
- **Failover / Fallback**：同语义换路继续 / 接受更差但可用的结果。
- **Circuit Breaker**：按错误率在 Closed/Open/Half-Open 间切换的快速失败机制。
- **Singleflight**：相同键的并发调用合并为一次实际执行。
- **数据面 / 控制面**：请求转发路径 / 配置与策略管理。
