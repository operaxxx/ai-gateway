# Design Context

> 由 dev-design-context 流程沉淀（2026-09-16），未来所有 UI 工作先读本文件。
> 完整设计说明见 `.claude/artifacts/designs/ui-redesign.md`，tokens 定义见 `static/style.css`。

## Design Context

### Users
开发者本人（单用户），在本地浏览器使用控制台完成三类高频任务：Prompt 模板维护（Jinja2 + 版本化）、LLM 调试调用（流式/深度思考/结构化输出）、调用观测（日志、历史对比）。使用场景是日间长时间阅读技术文本，桌面端为主。

### Brand Personality
冷静、精确、可靠。界面应传达「这是个可信的工程工具」：数据优先、装饰极少、反馈即时。

### Aesthetic Direction
- 参照：Linear / Vercel / Stripe Dashboard 式精密开发工具风。
- 反参照：数据密集监控大屏（Grafana 式高密度堆砌）、消费级聊天产品的圆润拟物感。
- 主题：浅色（浅冷灰底 + 白卡片），token 体系预留深色扩展位。
- 色彩：中性灰蓝阶 + 强调蓝 `#2e5ce6`；彩色只表达语义（成功/警示/危险），不做装饰。
- 动效：140ms 克制过渡，仅 hover/focus/状态切换；尊重 `prefers-reduced-motion`。

### Design Principles
1. **数据即界面**：技术数据（模型名、token、耗时、id）一律等宽字体 + `tabular-nums`，与正文无衬线严格区分。
2. **层次靠明度不靠色块**：hairline 边框 + 三档表面明度（bg/surface/surface-2）+ 极浅阴影表达结构；彩色只留给语义反馈。
3. **一屏一主操作**：每个面板仅一个实底主按钮，其余降级为 ghost/sm。
4. **状态永远可见**：异步过程（请求/流式/校验/错误）必须有即时、带色、可扫读的反馈。
5. **零构建约束**：纯原生 HTML/CSS/JS、无 CDN 无框架；新组件必须能仅靠 token 与原生元素实现。

### 硬性实现约定
- 全部样式值引用 `static/style.css :root` 的 token，不写裸值。
- `[hidden] { display: none !important }` 必须保留在 reset 区（组件 `display:flex` 会覆盖 UA 隐藏规则，曾因此出 bug）。
- `app.js` 通过 ID 绑定 DOM；改 HTML 时不得删除/重命名任何 id，JS 动态改写 textContent 的按钮（btn-tpl-save、btn-compare、logs-toggle 等）内部禁止放图标。
- 用户数据一律经 textContent/createElement 进 DOM（安全约定，见 app.js 头注释）。
